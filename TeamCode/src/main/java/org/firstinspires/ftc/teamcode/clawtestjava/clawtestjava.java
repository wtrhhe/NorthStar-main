/*package org.firstinspires.ftc.teamcode.clawtestjava;

import com.qualcomm.robotcore.eventloop.opmode.Disabled;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;
import org.firstinspires.ftc.teamcode.Drivetrain.TankDrivetrain;

@TeleOp(name = "Test Claw")
@Disabled
public class clawtestjava extends LinearOpMode {
    TankDrivetrain robot = new TankDrivetrain();*/
   /* @Override*/
    /*public void runOpMode(){
        robot.init(hardwareMap);

        waitForStart();

        robot.claw1.scaleRange(0, 1);
        robot.claw2.scaleRange(0, 1);
        robot.claw1.setPosition(0);
        robot.claw2.setPosition(0);

        while (opModeIsActive()) {
            if(gamepad1.a) {
                robot.claw1.setPosition(.1);
            }
            if(gamepad1.b) {
                robot.claw1.setPosition(1);
            }

            if(gamepad1.x) {
                robot.claw2.setPosition(.1);
            }
            if(gamepad1.y) {
                robot.claw2.setPosition(1);
        }

            telemetry.addData("claw1 Servo Position: ", robot.claw1.getPosition()); 
            telemetry.addData("claw2 Servo Position: ", robot.claw2.getPosition());
            telemetry.update();
        }
    }
}*/
